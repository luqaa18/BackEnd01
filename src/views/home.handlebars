<h1 style="text-align:center">HOME</h1>

<div class="divContainerHome">
  <table class="product-table productTableHome">
    <thead>
      <tr>
        <th>Producto</th>
        <th>Categoría</th>
        <th>Código</th>
        <th>Precio</th>
        <th>Stock</th>
      </tr>
    </thead>
    <tbody id="tbody">
      {{#each products}}
        <tr id="product-{{this.id}}">
          <td>
            <p class="product-name">{{this.title}}</p>
            <p class="product-details">{{this.description}}</p>
          </td>
          <td>{{this.category}}</td>
          <td>{{this.code}}</td>
          <td><p>${{this.price}}</p></td>
          <td>{{this.stock}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>